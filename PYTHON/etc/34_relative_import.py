# because 'constants' is in '../../constants.py'
from sys import path as sys_path
sys_path.append("..")

from constants import (
        ASTC_DIR, ASTC_REPO,
        ASTC_BRANCH, ASTC_URL,
        ASTC_SERVICE,
        )
